import React from 'react';
import classes from './ChatBlank.module.scss';

const ChatBlank = () => {
    return <span className={classes.notice}>Open any conversation to start a chat.</span>
};

export default ChatBlank;