import React from 'react';
import CurrentVideoPageContainer from '../../components/CurrentVideoPageComponents/CurrentVideoPageContainer';

const VideoCurrentPage = ({currentUser, token, currentVideo, ...props}) => {
    return <CurrentVideoPageContainer/>;
};

export default VideoCurrentPage;